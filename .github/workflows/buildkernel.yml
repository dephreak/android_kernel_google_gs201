name: Build Sultan GS201 Kernel

on:
  workflow_dispatch:    # lets you run it manually from the Actions tab
  push:
    branches:
      - 16.0.0-sultan   # run automatically when you push to this branch

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: 16.0.0-sultan

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            bc bison flex libssl-dev \
            libc6-dev libncurses5-dev \
            ccache git python3 \
            build-essential \
            dwarves \
            lz4 rsync

      - name: Build kernel (GKI aarch64)
        env:
          LTO: thin
          BUILD_CONFIG: build.config.gki.aarch64
        run: |
          chmod +x build/build.sh
          ./build/build.sh -j$(nproc) V=1

      - name: List build outputs
        run: |
          echo "==== dist/ ===="
          ls -R dist || echo "no dist dir"
          echo "==== out/ ===="
          ls -R out || echo "no out dir"

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: sultan-gs201-kernel-dist
          path: |
            dist/**
            out/**/dist/**
